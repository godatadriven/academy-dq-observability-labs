# Lab 4, Part A · Every reader of the payments table, named. The steps are in README.md.
# Three readers sit outside dbt: the revenue dashboard, the finance close, and the reorder agent.
# Types: dashboard (charts), notebook, analysis (a report process), ml (a model), application (a program that acts).
version: 2
exposures:
  - name: revenue_dashboard
    type: dashboard
    owner: {name: Sam}                 # Module 1: Sam in finance owns the definition of revenue
    depends_on: [ref('______')]        # the table the dashboard reads: see the diagram in the root README

  - name: finance_close
    type: ______                       # pick the closest, see the list above
    owner: {name: Sam}
    depends_on: [ref('revenue_daily')]

  - name: ingredient_reorder_agent
    type: ______                       # pick the closest, see the list above
    owner: {name: ______}              # one person's name, not a team. Yours is fine.
    depends_on: [ref('______')]        # it reads the payments table itself. See agent_tools.py
