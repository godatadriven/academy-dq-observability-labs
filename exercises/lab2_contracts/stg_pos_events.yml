# Lab 2 · A gate on the pipe. The steps are in README.md.
# A contract lists the columns the copy promises, with their types.
# Blank 1: turn the contract on (true or false). Blank 2: the rule that says amount can never be empty.
version: 2
models:
  - name: stg_pos_events
    config:
      contract:
        enforced: ___
    columns:
      - name: payment_id
        data_type: integer
      - name: customer_id
        data_type: integer
      - name: cafe_id
        data_type: varchar
      - name: payment_method
        data_type: varchar
      - name: amount
        data_type: integer
        constraints:
          - type: ______
      - name: paid_at
        data_type: timestamp
      - name: _etl_loaded_at
        data_type: timestamp
